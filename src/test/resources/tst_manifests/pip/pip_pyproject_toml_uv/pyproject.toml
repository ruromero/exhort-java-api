[project]
name = "test-project"
version = "0.1.0"
dependencies = [
    "anyio==3.6.2",
    "flask==2.0.3",
    "requests==2.25.1",
]

[project.optional-dependencies]
dev = [
    "click==8.0.4",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0",
]
